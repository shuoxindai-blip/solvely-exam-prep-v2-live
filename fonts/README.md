# Fonts

No source font files were present in the captured evidence.

`colors_and_type.css` therefore uses system UI stacks compatible with iOS and web app surfaces. If future evidence includes `.woff2`, `.woff`, `.otf`, or `.ttf` files, place them here and bind them with `@font-face` in `colors_and_type.css`.
